# 修正生成器后的三圈完整 DE 复核

2026-09-23，使用 0.4.0 的接触源检查、完整逆推算符策略和当前 symmetry 实现，重新生成关系并重构完整有理 DE。沿用此前独立计算得到的基定义作为候选输入；没有导入旧方程行、旧约化规则或旧 symmetry 缓存。此次是固定候选基的完整复核，不是再次迭代寻找基。

| 系统 | 三圈 | 二圈 | 一圈 | 完整维数 | 精确曲率零项 | 全池单点查询核对 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| ladder / TierReference + PreserveActualSpan | 19 | 7 | 4 | 30 | 900/900 | 91/91 |
| tennis / O7 + PreserveActualSpan | 31 | 11 | 4 | 46 | 2116/2116 | 142/142 |

全部基均为单独有限积分，没有隐藏的组合基。两个原始 top 分别保留为第 3、第 18 项。完整曲率包含高圈到低圈源项；横向和纵向低圈拓扑均使用相同外部运动学。维数指当前已验证关系池下的有限基，不声称全局最小 MI 数。

24 workers 重新执行 708301 个历史 seed/operator 应用，分成 180 个任务。不能通过当前接触项、域和标量积检查的应用均记录拒绝原因，未截去源项后保留。随后 ladder 针对 6 个残留三圈原子补齐 7495 次新作用，新增 3103 条关系，并重新约化原 top 和所有候选基导数。最终三圈池分别有 60506、295500 条关系；映射并去重后的完整混合池分别有 61629、299521 条关系。

ladder 的 30 维矩阵与旧矩阵完全一致。tennis 的原 47 个候选中，第 33 项在新联合池中仍被约化为其他基；此依赖的导数关系也精确成立，消去后为 46 维。其证明来自重新生成的高低圈联合池，不能描述成仅用二圈池得到的独立证明。消去前的 47 维候选矩阵有 93 个元素不同于旧结果。旧有矛盾池和旧矩阵继续隔离，不能以新结果替旧工件背书。

有理重构使用 FiniteFlow 的多次采样；独立全池残差核对使用一个 generic 点：
`{7338376896737913617,7718278679044643367}`，素数 `9223372036854775783`。全部系数成功求值并与全池查询一致；这项数值核对与完整曲率的逐项精确零检查分开记录。

## 文件

- `*-DifferentialEquations.wl`：完整基、x/y 矩阵、原 top 的坐标和验收记录。
- `*-verification.wl/json`：覆盖、有限性、曲率、依赖求导和哈希一致性检查。
- `tennis-LowerDependency.wl`：从旧 47 项候选中消去第 33 项的具体关系。
- `SourceManifest.json`：实际用于生成关系的冻结源码指纹。
- `manifest.json`：工件 SHA256、规模和本地完整运行目录。

先加载 `Kernel/ConformalIBP.wl`，再用 `Get` 读取 WL 工件，以保持符号 context。`Basis` 中的 G 分别有 18、11、5 个指标；`PhysicalBasis` 保留此前表达法。`Placeholders` 是矩阵坐标标签。微分约定为 `D[B,z] = Matrices[z].B`。

重做导出矩阵的精确曲率检查：

```sh
python3 reproduction/corrected-three-loop-20260923/verify-curvature.py --kernel WolframKernel --workers 24
```

此便携脚本仅重验导出矩阵的精确曲率。完整重生成、FiniteFlow 重构和全池单点对照的脚本、种子账本、大型方程池保存在 manifest 标明的本地运行目录，没有打包进 Git。该三圈结果不代表四圈碰撞处方或有限性判据已完备。
